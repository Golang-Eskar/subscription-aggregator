# subscription-aggregator
Subscription Aggregator — REST-сервис, позволяющий хранить и управлять данными о пользовательских онлайн-подписках, а также агрегировать их стоимость за выбранный период. Сервис реализован на Go, использует PostgreSQL как хранилище и предоставляет HTTP API с документацией в формате Swagger. Запуск выполняется через Docker Compose.

# 🧾 Subscription Aggregator (Go + PostgreSQL + Docker)

**Subscription Aggregator** — это REST-сервис для хранения, управления и анализа онлайн-подписок пользователей.  
Сервис поддерживает CRUD-операции над записями о подписках и подсчёт их суммарной стоимости за выбранный период.

---

## 🚀 Возможности

- CRUDL-операции над записями о подписках:
  - создание, чтение, обновление, удаление, получение списка
- Подсчёт суммарной стоимости подписок за период
- Фильтрация по пользователю и названию сервиса
- Хранение данных в PostgreSQL
- Логирование всех операций
- Конфигурация через `.env` / `config.yaml`
- Документация API через Swagger
- Легкий запуск через `docker-compose`

---

## 🧱 Технологии

- **Go (Golang)** — основной язык
- **Gin / Chi / Fiber** — HTTP-фреймворк (на выбор)
- **PostgreSQL** — база данных
- **golang-migrate / goose** — миграции
- **zap / zerolog** — логирование
- **swaggo/swag** — Swagger документация
- **Docker Compose** — инфраструктура

---

## 📂 Структура проекта

subscription-aggregator/
├── cmd/
│ └── app/
│ └── main.go
├── internal/
│ ├── config/ # конфигурация приложения
│ ├── controller/ # HTTP-контроллеры
│ ├── service/ # бизнес-логика
│ ├── repository/ # доступ к БД
│ ├── model/ # модели данных
│ └── logger/ # логирование
├── migrations/ # SQL-миграции
├── pkg/ # вспомогательные пакеты
├── tests/ # юнит- и интеграционные тесты
├── .env # переменные окружения
├── Dockerfile
├── docker-compose.yml
├── Makefile
└── README.md



